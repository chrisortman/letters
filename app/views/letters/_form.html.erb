<%= form_for(@letter) do |f| %>
  <% if @letter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@letter.errors.count, "error") %> prohibited this letter from being saved:</h2>

      <ul>
      <% @letter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p> 
    Share your story of what God's love has done for you
  </p>

  <div class="form-group">
    <%= f.label :text, :class => ['sr-only'] %>
    <%= f.text_area :text, :class => 'form-control', :rows => 25 %>
  </div>

  <div class="form-group">
    <%= f.label :from, :class => 'control-label' %><br>
    <%= f.text_field :from, :class => 'form-control' %>
  </div>

  <div class="actions">
    <%= f.submit :class => 'btn btn-primary' %>
  </div>
<% end %>
